configfile: "config/config.yaml"

DATA_DIR = config['data_dir']

rule all:
    input:
        expand("{DATA_DIR}/C.txt", DATA_DIR=DATA_DIR)
    output:
        expand("{DATA_DIR}/wc_D.txt", DATA_DIR=DATA_DIR)
    shell:
        "wc -w {input} > {output}"

